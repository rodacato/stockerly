# What the conventions in script/checks/ already find on master, so each one can
# be green while still failing anything new. Same bargain as .rubocop_todo.yml:
# debt to shrink, not a permanent exemption. A row whose count the code has
# fallen below is reported as stale — trim it rather than leave it padding.
#
# `count` is per file. Exceed it and the run fails.

design-tokens:
  # #489 counted these; #548, #561 and the cockpit pass migrated or deleted the
  # rest. What remains is the flash box's green/red pair and the watchlist
  # button's emerald, both older than the token set.
  app/views/shared/_flash_message.html.erb:
    count: 1
  app/views/watchlist_items/_watchlist_button.html.erb:
    count: 1

# Empty on purpose: #541 picked signed_percent, so the five files this started
# with are migrated rather than exempted. Keep the key so a new finding lands
# under a heading that already reads as debt.
number-format:

# Empty on purpose: the one row this convention started with (#570) is fixed,
# not exempted. Keep the key so a new finding lands under a heading that
# already reads as debt.
use-cases:

# Empty on purpose: the one row this convention started with (#560) is fixed,
# not exempted. SplitDetected carries the split's facts now, so Trading's
# handler resolves no MarketData row. Keep the key so a new finding lands under
# a heading that already reads as debt.
boundaries:
