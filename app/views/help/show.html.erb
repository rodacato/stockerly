<% content_for(:title, "Ayuda y soporte") %>

<main class="flex-1 flex flex-col items-center py-12 px-4">
  <div class="w-full max-w-2xl space-y-8">
    <%# Header %>
    <div>
      <h1 class="text-3xl font-bold text-fg-default tracking-tight">Ayuda y soporte</h1>
      <p class="text-sm text-fg-subtle mt-2">Cómo empezar con Stockerly, qué hacer cuando algo falla, y cómo contactarnos.</p>
    </div>

    <%# Same body as /welcome, no CTA %>
    <%= render "shared/welcome_body" %>
  </div>
</main>
