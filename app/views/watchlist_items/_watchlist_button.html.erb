<%# In-watchlist state for an asset's watchlist toggle, swapped in via Turbo Stream. %>
<%= turbo_frame_tag "watchlist_button_#{asset_id}" do %>
  <span class="inline-flex items-center gap-1 text-emerald-500 text-xs font-medium">
    <%= icon_svg "check_circle", class: "text-lg" %>
  </span>
<% end %>
