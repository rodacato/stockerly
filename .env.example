# Database (devcontainer defaults)
DATABASE_HOST=db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# Rails
RAILS_ENV=development
RAILS_MAX_THREADS=5

# Active Record Encryption (generate with: bin/rails db:encryption:init)
# ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=
# ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=
# ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=

# Market data API keys — configure via Admin > Integrations (NOT read from ENV)
# Providers: Polygon.io, CoinGecko, ExchangeRate, Alpha Vantage, Banxico, FMP

# Error tracking (Honeybadger)
# HONEYBADGER_API_KEY=           # get from https://app.honeybadger.io

# Email (Resend SMTP)
# RESEND_API_KEY=                # get from https://resend.com/api-keys

# Deployment (only needed for Kamal deploy)
# SECRET_KEY_BASE=        # generate with: bin/rails secret
# DATABASE_URL=
# POSTGRES_PASSWORD=
# HOST_IP=
# KAMAL_REGISTRY_PASSWORD=
