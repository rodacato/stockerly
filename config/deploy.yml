service: stockerly
image: rodacato/stockerly

registry:
  username: rodacato
  password:
    - KAMAL_REGISTRY_PASSWORD

servers:
  web:
    hosts:
      - 46.225.67.253
  job:
    hosts:
      - 46.225.67.253
    cmd: bundle exec rails solid_queue:start

proxy:
  ssl: true
  host: stockerly.notdefined.dev
  app_port: 3000
  healthcheck:
    interval: 3
    path: /up
    timeout: 3

env:
  clear:
    RAILS_SERVE_STATIC_FILES: true
    RAILS_LOG_TO_STDOUT: true
    DB_HOST: stockerly-postgres
    POSTGRES_USER: stockerly
    POSTGRES_DB: stockerly_production
    SOLID_QUEUE_IN_PUMA: false
  secret:
    - RAILS_MASTER_KEY
    - POSTGRES_PASSWORD
    - DATABASE_URL

builder:
  arch: amd64
  args:
    RAILS_ENV: production
  cache:
    type: registry
    options: mode=max
    image: rodacato/stockerly:build-cache

accessories:
  postgres:
    image: postgres:16
    host: 46.225.67.253
    env:
      clear:
        POSTGRES_USER: stockerly
        POSTGRES_DB: stockerly_production
      secret:
        - POSTGRES_PASSWORD
    directories:
      - data:/var/lib/postgresql/data
    options:
      restart: unless-stopped
